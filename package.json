{
  "name": "zhibo-wang-engineering-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal engineering portfolio for Zhibo Wang.",
  "scripts": {
    "start": "eleventy --serve",
    "dev": "eleventy --serve",
    "build": "eleventy && pagefind --site _site",
    "preview:search": "pagefind --site _site --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.2",
    "pagefind": "1.5.2"
  }
}
